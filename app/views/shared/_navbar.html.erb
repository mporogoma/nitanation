<nav class="bg-red-800 border-b border-red-900">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex items-center justify-between h-16">
      <div class="flex items-center">
        <div class="flex-shrink-0">
          <span class="text-white font-bold text-xl">Sales Manager</span>
        </div>
        <div class="hidden md:block">
          <div class="ml-10 flex items-baseline space-x-4">
            <%= link_to 'Dashboard', current_user.admin? ? admin_dashboard_path : seller_dashboard_path, class: "text-yellow-300 hover:bg-red-700 hover:text-white px-3 py-2 rounded-md text-sm font-medium" %>
            <%= link_to 'Products', products_path, class: "text-yellow-300 hover:bg-red-700 hover:text-white px-3 py-2 rounded-md text-sm font-medium" %>
            <%= link_to 'Daily Sales', daily_sales_path, class: "text-yellow-300 hover:bg-red-700 hover:text-white px-3 py-2 rounded-md text-sm font-medium" %>
            <% if current_user&.admin? %>
              <%= link_to 'Categories', categories_path, class: "text-yellow-300 hover:bg-red-700 hover:text-white px-3 py-2 rounded-md text-sm font-medium" %>
              <%= link_to 'Users', users_path, class: "text-yellow-300 hover:bg-red-700 hover:text-white px-3 py-2 rounded-md text-sm font-medium" %>
            <% end %>
          </div>
        </div>
      </div>
      
      <% if user_signed_in? %>
        <div class="hidden md:block">
          <div class="ml-4 flex items-center md:ml-6">
            <div class="ml-3 relative">
              <div>
                <button type="button" class="max-w-xs bg-red-800 rounded-full flex items-center text-sm focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-offset-red-800 focus:ring-white" id="user-menu" aria-expanded="false" aria-haspopup="true">
                  <span class="sr-only">Open user menu</span>
                  <div class="h-8 w-8 rounded-full bg-gold-500 flex items-center justify-center text-white font-bold">
                    <%= current_user.email.first.upcase %>
                  </div>
                </button>
              </div>
              <div class="origin-top-right absolute right-0 mt-2 w-48 rounded-md shadow-lg py-1 bg-white ring-1 ring-black ring-opacity-5 focus:outline-none hidden" role="menu" aria-orientation="vertical" aria-labelledby="user-menu">
                <%= link_to 'Profile', edit_user_registration_path, class: "block px-4 py-2 text-sm text-gray-700 hover:bg-gray-100" %>
                <%= link_to 'Sign out', destroy_user_session_path, method: :delete, class: "block px-4 py-2 text-sm text-gray-700 hover:bg-gray-100" %>
              </div>
            </div>
          </div>
        </div>
      <% end %>

      <div class="-mr-2 flex md:hidden">
        <button type="button" class="bg-red-800 inline-flex items-center justify-center p-2 rounded-md text-gold-400 hover:text-white hover:bg-red-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-offset-red-800 focus:ring-white" aria-controls="mobile-menu" aria-expanded="false" id="mobile-menu-button">
          <span class="sr-only">Open main menu</span>
          <svg class="block h-6 w-6" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
          </svg>
          <svg class="hidden h-6 w-6" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
          </svg>
        </button>
      </div>
    </div>
  </div>

  <!-- Mobile menu -->
  <div class="md:hidden hidden" id="mobile-menu">
    <div class="px-2 pt-2 pb-3 space-y-1 sm:px-3">
      <%= link_to 'Dashboard', current_user.admin? ? admin_dashboard_path : seller_dashboard_path, class: "text-yellow-300 hover:bg-red-700 hover:text-white block px-3 py-2 rounded-md text-base font-medium" %>
      <%= link_to 'Products', products_path, class: "text-yellow-300 hover:bg-red-700 hover:text-white block px-3 py-2 rounded-md text-base font-medium" %>
      <%= link_to 'Daily Sales', daily_sales_path, class: "text-yellow-300 hover:bg-red-700 hover:text-white block px-3 py-2 rounded-md text-base font-medium" %>
      <% if current_user&.admin? %>
        <%= link_to 'Categories', categories_path, class: "text-yellow-300 hover:bg-red-700 hover:text-white block px-3 py-2 rounded-md text-base font-medium" %>
        <%= link_to 'Users', users_path, class: "text-yellow-300 hover:bg-red-700 hover:text-white block px-3 py-2 rounded-md text-base font-medium" %>
      <% end %>
    </div>
    <% if user_signed_in? %>
      <div class="pt-4 pb-3 border-t border-red-700">
        <div class="flex items-center px-5">
          <div class="flex-shrink-0">
            <div class="h-10 w-10 rounded-full bg-gold-500 flex items-center justify-center text-white font-bold">
              <%= current_user.email.first.upcase %>
            </div>
          </div>
          <div class="ml-3">
            <div class="text-base font-medium text-white"><%= current_user.email %></div>
            <div class="text-sm font-medium text-yellow-300"><%= current_user.roles.first.name.titleize %></div>
          </div>
        </div>
        <div class="mt-3 px-2 space-y-1">
          <%= link_to 'Profile', edit_user_registration_path, class: "block px-3 py-2 rounded-md text-base font-medium text-yellow-300 hover:text-white hover:bg-red-700" %>
          <%= link_to 'Sign out', destroy_user_session_path, method: :delete, class: "block px-3 py-2 rounded-md text-base font-medium text-yellow-300 hover:text-white hover:bg-red-700" %>
        </div>
      </div>
    <% end %>
  </div>
</nav>